import React, { useState } from 'react';
import { Search, Menu, X } from 'lucide-react';

const Opportunities = () => {
  const [menuOpen, setMenuOpen] = useState(false);

  const toggleMenu = () => {
    setMenuOpen(!menuOpen);
  };

  return (
    <>
      {/* Navbar */}
      <nav className="w-full flex flex-row  justify-between  bg-gradient-to-r from-blue-500 via-indigo-600 to-purple-700 shadow-lg text-white border-b-4 border-purple-900">
        <div className="mx-auto flex flex-row w-full justify-around items-center px-4 py-3">
          {/* Logo */}
          <div className="text-xl font-bold flex items-center gap-2">
            <img src="./DevDisplay ICON.png" alt="DevDisplay" className="h-12 w-12 rounded-full shadow-md" />
            <span className="text-white tracking-wider">DevDisplay</span>
          </div>

          {/* Hamburger Menu for Mobile */}
          <button
            className="md:hidden text-white focus:outline-none"
            onClick={toggleMenu}
          >
            {menuOpen ? <X className="h-6 w-6" /> : <Menu className="h-6 w-6" />}
          </button>
        </div>

        {/* Navbar Links and Search */}
        <div
          className={`md:flex md:items-center md:gap-4 md:static absolute  top-0 left-0 w-full bg-gradient-to-r from-indigo-600 to-purple-700 transition-transform duration-300 ease-in-out ${menuOpen
              ? 'transform translate-y-0 top-14'
              : 'transform -translate-y-full'
            } md:transform-none md:translate-y-0 md:top-auto md:py-0 py-6`}
        >
          <div className="flex flex-col md:flex-row items-center gap-4 px-4">
            {/* Select Dropdown */}
            <select className="rounded-md p-2 bg-white text-blue-600 shadow-md hover:bg-blue-100 focus:outline-none transition duration-200 md:mx-2">
              <option value="1">Hybrid & Onsite Jobs</option>
              <option value="2">Remote Jobs</option>
              <option value="3">Internships</option>
              <option value="4">Freelance Work</option>
              <option value="5">Hackathons</option>
              <option value="6">Open-Source Projects</option>
            </select>

            {/* Search Bar */}
            <div className="relative w-full md:w-72 md:mx-2">
              <input
                type="text"
                placeholder="Search opportunities..."
                className="w-full rounded-l-md text-gray-800 bg-slate-100 p-2 shadow-inner focus:outline-none focus:ring focus:ring-blue-300 transition duration-200"
              />
              <button className="absolute right-0 top-0 h-full bg-blue-500 text-white rounded-r-md p-2 hover:bg-blue-600 transition duration-200">
                <Search />
              </button>
            </div>
          </div>
        </div>
      </nav>

      <nav  className="w-full flex flex-row  justify-between  bg-gradient-to-r from-blue-500 via-indigo-600 to-purple-700 shadow-lg text-white border-b-4 border-purple-900">
            <div>
              <h1>Sort By:</h1>
            </div>
      </nav>

      
    </>
  );
};

export default Opportunities;
